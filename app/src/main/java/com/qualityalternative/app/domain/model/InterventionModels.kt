package com.qualityalternative.app.domain.model

enum class InterventionAction {
    PRIMARY_ACCEPTED,
    BACKUP_ACCEPTED,
    DELAY_SELECTED,
    DELAY_ALTERNATIVE_STARTED,
    OPEN_ANYWAY_SELECTED,
}

data class InterventionDecision(
    val action: InterventionAction,
    val targetAppPackage: String,
    val contentId: String? = null,
    val occurredAtMillis: Long,
)

data class SessionFeedback(
    val wasGoodFit: Boolean,
    val helpedAvoidScrolling: Boolean,
    val fitRating: String? = null,
    val scrollRating: String? = null,
    val submittedAtMillis: Long,
)

enum class AnalyticsEventType {
    TARGET_APP_FOREGROUND_DETECTED,
    INTERVENTION_SHOWN,
    INTERVENTION_DEGRADED_PERFORMANCE,
    NO_RECOMMENDATION_AVAILABLE,
    PRIMARY_ACCEPTED,
    BACKUP_ACCEPTED,
    DELAY_SELECTED,
    DELAY_ALTERNATIVE_STARTED,
    OPEN_ANYWAY_SELECTED,
    FORM_INTERVENTION_SHOWN,
    FORM_INTERVENTION_UNLOCK_BLOCKED,
    FORM_INTERVENTION_UNLOCK_ENABLED,
    FORM_INTERVENTION_UNLOCK_USED,
    FORM_INTERVENTION_COMPLETED,
    FORM_INTERVENTION_ABANDONED,
    BEDTIME_INTERVENTION_SHOWN,
    BEDTIME_UNLOCK_BLOCKED,
    BEDTIME_UNLOCK_ENABLED,
    BEDTIME_UNLOCK_USED,
    READER_COMPLETED,
    READER_SKIPPED,
    FEEDBACK_SUBMITTED,
    INVENTORY_SHORTAGE,
    RETURN_TO_APP_WITHIN_15_MINUTES,
    RETURN_TO_APP_WITHIN_60_MINUTES,
    RETURN_AFTER_DELAY_ENDED,
    MEDITATION_TIMER_COMPLETED,
    MEDITATION_TIMER_SKIPPED,
    USER_LINK_ADDED,
    USER_DOCUMENT_ADDED,
    USER_DOCUMENT_BODY_LOAD_FAILED,
    USER_LINK_FALLBACK_OPENED,
    USER_LINK_HANDOFF_FAILED,
    EXTERNAL_HANDOFF_OPENED,
    EXTERNAL_HANDOFF_FAILED,
    PRIORITY_CONTENT_TOGGLED,
    USER_CONTENT_DELETED,
    BATCH_DOCUMENT_IMPORT_ATTEMPTED,
    BATCH_DOCUMENT_IMPORT_COMPLETED,
    READING_TIME_ESTIMATE_APPLIED,
    PRIORITY_SET_DURING_ADD,
    READING_PROGRESS_SAVED,
    MANUAL_CONTINUE_STARTED,
    UNFINISHED_CONTENT_RECOMMENDED_AS_PRIMARY,
    COMPLETED_CONTENT_ACTIVATION_TOGGLED,
    READING_ANNOTATION_CREATED,
    READING_ANNOTATION_UPDATED,
    READING_ANNOTATION_DELETED,
    ANNOTATION_DRIVE_SYNC_CONNECTED,
    ANNOTATION_DRIVE_SYNC_SUCCEEDED,
    ANNOTATION_DRIVE_SYNC_FAILED,
    ANNOTATION_DRIVE_SYNC_DISCONNECTED,
    AGENT_INBOX_CONNECTED,
    AGENT_INBOX_SCAN_SUCCEEDED,
    AGENT_INBOX_SCAN_FAILED,
    AGENT_INBOX_CANDIDATE_DETECTED,
    AGENT_INBOX_CANDIDATE_IMPORTED,
    AGENT_INBOX_CANDIDATE_IMPORT_FAILED,
    AGENT_INBOX_CANDIDATE_DUPLICATE,
    AGENT_INBOX_CANDIDATE_REJECTED,
    AGENT_INBOX_PRIORITY_TOGGLED,
    AGENT_INBOX_IMPORT_ALL_STARTED,
    AGENT_INBOX_IMPORT_ALL_COMPLETED,
    AGENT_INBOX_AUTOIMPORT_TOGGLED,
    AGENT_INBOX_DISCONNECTED,
}

data class AnalyticsEvent(
    val type: AnalyticsEventType,
    val timestampMillis: Long,
    val semanticKey: String? = null,
    val interventionId: String? = null,
    val sessionId: String? = null,
    val targetAppPackage: String? = null,
    val primaryContentId: String? = null,
    val backupContentIds: List<String> = emptyList(),
    val contentId: String? = null,
    val metadata: Map<String, String> = emptyMap(),
)
