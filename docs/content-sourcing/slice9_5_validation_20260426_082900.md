# Slice 9.5 Validation

Date: 2026-04-26

## Checks

- Added 8 candidate rows with `vertical_slice = 9.5`.
- Confirmed Slice 9.5 mix: 6 `RENDERABLE`, 2 `LINK_ONLY`.
- Confirmed all Slice 9.5 rows use `replacement_moment = CREATIVITY_PLAY`.
- Confirmed all Slice 9.5 rows use `primary_topic = CREATIVITY`.
- Confirmed full Sprint 9 pool now contains 100 candidates.
- Confirmed full Sprint 9 rights mix is 42 renderable and 58 link-only.
- Confirmed all Slice 9.5 link-only rows use `EXTERNAL_HANDOFF`.
- Confirmed all Slice 9.5 link-only rows keep `must_not_scrape_cache_or_summarize = true`.
- Confirmed all Slice 9.5 renderable rows remain `rights_pending`.
- Confirmed no Slice 9.5 rows are approved for future integration.
- Confirmed no modern source family exceeds the 10-row full-pool cap.

## Scope Guardrails

- No edits to `starter_packs.json`.
- No runtime app behavior changes.
- No scraping, caching, rehosting, reader-mode extraction, or AI summary behavior.
- No modern third-party article text was added to app assets.

## Distribution Note

The original Sprint 9 plan sketched Slice 9.5 as 3 renderable and 5 link-only rows. After earlier reviewed slice mix changes, the actual remaining target after Slice 9.4 was 6 renderable and 2 link-only rows. Slice 9.5 intentionally uses 6/2 so the final pool reaches the target 42 renderable and 58 link-only candidates.

The original duration target was 40 candidates at 3-5 minutes, 40 at 5-10 minutes, and 20 at 10-20 minutes. The final sourced pool does not meet that target: it has 6 candidates at 3-5 minutes, 63 at 6-10 minutes, and 31 at 11-20 minutes. This deviation is documented for final review and does not approve any candidate for app integration.

## Final Review Fixes

- Replaced the prior `s9-1-r03` William James row because its canonical URL overlapped the existing `attention-comes-in-beats` starter-pack item.
- Replaced the prior `s9-1-r04` John Stuart Mill row because its canonical URL overlapped the existing `choose-your-own-plan` starter-pack item.
- Confirmed the full 100-row pool no longer reuses any canonical URL already present in `starter_packs.json`.
