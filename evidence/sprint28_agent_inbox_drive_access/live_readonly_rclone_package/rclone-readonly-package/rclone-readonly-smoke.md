# Rclone Readonly Inbox Smoke

This private Markdown package was uploaded by rclone after the Android app already existed.

The expected Sprint 28 result is that the app can scan the user-supplied Agent Inbox folder id through the controlled Drive readonly fallback and show this package for review.
