# Sprint 38 Autoimport Startup

This live Google Drive package proves that opt-in autoimport can import a newly uploaded package after the app is relaunched.

The app should scan the configured Agent Inbox folder on startup without launching a new approval prompt when Drive access has already been granted.
